/**
 * content.js — VLC YouTube Launcher
 *
 * Content script injected into YouTube pages.
 * Currently a thin stub; all data extraction is done
 * via chrome.scripting.executeScript in popup.js for
 * cleaner lifecycle management.
 *
 * This file is declared in manifest.json and acts as a
 * placeholder for future enhancements (e.g., context menu
 * integration, keyboard shortcut listener, etc.).
 */

// No active logic needed here — data extraction is handled
// directly by popup.js via chrome.scripting.executeScript.
